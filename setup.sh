# Setup commands
# prep
# This might be needed for jabbapylib. pycurl fails. 
sudo apt install libcurl4-openssl-dev libssl-dev
sudo apt install cmake
sudo apt-get install qt4-default
pip install pyside
pip install jabbapylib


